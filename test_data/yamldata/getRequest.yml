-   name: /search/history
    request:
        headers:
            "Content-Type": "application/json"
        method: GET
        params:
            words: Test
        url: /search/history
    validate:
        status_code: 200


-   name: /search/history
    request:
        headers:
            "Content-Type": "application/json"
        method: GET
        params:
            words: Python
        url: /search/history
    validate:
        status_code: 200
