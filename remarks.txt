Python+Requests+Pytest+Logbook+Allure
文件目录
        base（基础操作）
                base_request（Get/Post请求）
	config（配置文件）
                server.ini（接口地址及配置文件）
        log（测试日志）
                log-2020-06-06.log（测试日志）
        reports（测试报告）
        test_case（测试数据）
                conftest
                test_getrequest
                test_postrequest
	test_data（测试数据）
	    exceldata（excel数据文件）
	    jsondata（json数据文件）
	    yamldata（yaml数据文件）
	test_main（主运行）
        run_main.py
	util（工具文件）
		handle_json（处理json）
		handle_db（处理数据库）（未做）
		handle_header（处理header）
		handle_cookie（处理cookie）（未做）
		handle_init（处理配置）
        handle_yaml（处理yml文件）
        handle_operationexcel（处理excel文件）
        handle_exceldata（处理excel文件）
        handle_excelconfig（处理excel文件）
        handle_log（处理log日志）