Python+Requests+Pytest+Logbook+Allure
文件目录
	base（基础操作）
        base_request.py（Get/Post请求）
	config（配置文件）
        server.ini（接口地址及配置文件）
    log（测试日志）
        log-2020-06-06.log（测试日志）
    reports（测试报告）
    test_case（测试数据）
        conftest.py
        test_one.py
	test_data（测试数据）
        接口Json文件
	test_main（主运行）
	util（工具文件）
		handle_json（处理json）
		handle_db（处理数据库）
		handle_header（处理header）
		handle_cookie（处理cookie）
		handle_init（处理配置）
        log（处理日志）