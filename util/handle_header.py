import sys
import os
import configparser


class HandleHeader:
    # 读取json文件中的header值
    def load_header():
        return ""

    # 获取header值，json文件中读取不到，则返回默认header值
    def get_header():
        return ""